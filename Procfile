# Fallback process definition for buildpack-based platforms (Railway Nixpacks,
# Heroku, Render native runtime). Ignored when the Dockerfile is used, which is
# Railway's default when one is present — kept so the project deploys either way.
web: sh scripts/start.sh
